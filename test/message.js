// ✅ Save monthly records separately for each swine
                // for (const swine of response.data.newSwines) {
                //     await api.post('/swine/add-monthly-record', {
                //         monthlyStatus: swine.status,
                //         monthlyWeight: swine.weight,
                //         swineId: swine._id
                //     });
                // }